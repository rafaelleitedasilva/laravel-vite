# --- Public (exposed to the browser) ------------------------------------------
# Canonical production URL. Used for metadataBase, canonical URLs, sitemap, OG.
NEXT_PUBLIC_SITE_URL="https://rafaelleitedasilva.dev.br"

# --- Contact form (server-only) ---------------------------------------------
# Resend API key — https://resend.com/api-keys
RESEND_API_KEY=""
# Verified sender. While the domain is not verified in Resend, use "onboarding@resend.dev".
CONTACT_FROM_EMAIL="onboarding@resend.dev"
# Where contact messages are delivered.
CONTACT_TO_EMAIL="rafael.leite.14@hotmail.com"

# --- Rate limiting (server-only, optional) ---------------------------------
# Upstash Redis REST credentials — https://upstash.com
# If unset, the /api/contact route still works but is NOT rate limited.
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
